[server]
port = 8082

[cache]
refresh_interval_seconds = 300  # 5 minutes

[location]
name = "Ghent"
latitude = 51.0543
longitude = 3.7174
radius_km = 160

[filters]
# Optional: pre-filter what gets stored at all
# Leave empty to store everything OCM returns
connector_types = []
networks = []